# Optional. Without it, the server falls back to unauthenticated GitHub API
# calls (60 requests/hour). A token raises that to 5,000/hour.
# Needs no scopes for public repos — just a plain classic or fine-grained
# token with default (read-only) access. This server never writes to GitHub.
GITHUB_TOKEN=
